<!doctype html>
<html>
<head>
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
    <title>Tryouts! </title>
    <link rel="icon" type="image/x-icon" href="https://cdn.discordapp.com/attachments/869334941337546782/1036327824862220378/Method_Draw_Image.png">
</head>
<body>
    <div class="bg-blue-200 min-h-screen flex items-center bg-gradient-to-tr from-blue-200 to-blue-400 scrollbar">
    <div class="w-full">
        <h2 class="text-center text-white font-bold text-2xl uppercase mb-10 drop-shadow-md" id="custom">
            404 NOT FOUND
        </h2>
        <div class="bg-white p-10 rounded-lg shadow md:w-3/4 mx-auto lg:w-1/2">
            <p class="font-semibold text-lg" >This Could be caused for different reasons:</p>
            <ul class="list-disc" >
                <li class="ml-8">The <span class="text-yellow-600 ">`secrets.json`</span> File was not found. Or its content is invalid</li>
                <li class="ml-8">An API call from the previous page failed 
                    <span id="couldBeAPI" class="font-light hidden">(It could be the <a class="underline" href="/">Home</a> page)</span>.
                </li>
                <li class="ml-8">You entered an invalid URL
                    <span id="couldBeURL" class="font-light hidden">(Previous url: «<span class="">/</span>»)</span>.
                </li>
                {# <li class="ml-8"><span class="text-yellow-600 ">`secrets.json`</span> File was not found.</li>
                <li class="ml-8"><span class="text-yellow-600 ">`secrets.json`</span> File was not found.</li> #}
            </ul>   
            {# <form action="">
                <div class="mb-5">
                <label for="content" class="block mb-2 font-bold text-gray-600">Desired Text</label>
                <input type="text" id="content" name="content" placeholder="Lorem ipsum dolor sit amet consectetur." 
                        class="border border-gray-300 shadow p-3 w-full rounded mb-">
                </div>          

                <button class="block w-full bg-blue-500 text-white font-bold p-4 rounded-lg" onClick="getImage()">Submit</button>            
            </form> #}
        </div>
    </div>
    </div>
<script>
    window.onload = function(){

        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.has("apiURL")) {            
            const couldBeAPI = document.getElementById("couldBeAPI");
            const couldBeAPIurl = document.querySelector("#couldBeAPI a");

            couldBeAPI.classList.remove("hidden");
            couldBeAPIurl.textContent= urlParams.get("apiName");
            couldBeAPIurl.href= urlParams.get("apiURL");
        }
        if (urlParams.has("url")) {
            const couldBeURL= document.getElementById("couldBeURL");
            const couldBeURLspan= document.querySelector("#couldBeURL span");

            couldBeURL.classList.remove("hidden");
            couldBeURLspan.textContent= urlParams.get("url");
            
        }

    }
</script>
</body>
</html>